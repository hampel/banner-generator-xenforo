# Normalise line endings: the working tree may pick up CRLF when committed through a
# Windows client over \\wsl$, but what is stored stays LF.
* text=auto eol=lf

*.png    binary
*.jpg    binary
*.gif    binary
